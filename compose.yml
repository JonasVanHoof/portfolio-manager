version: '3.4'

services:
  dashboard:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - '4200:4200'
    volumes:
      - ./:/usr/src/app
    networks:
      - portfolio-manager

networks:
  portfolio-manager:
    name: portfolio-manager
    external: true
