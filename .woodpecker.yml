when:
  - event: push
    branch:
      - main

steps:
  test:
    image: ${ci_docker_build_tag}:1.0.0
    on-error: ignore
    commands:
      - npm install
      - ember test
  run:
    image: ${ci_docker_build_tag}:1.0.0
    on-error: ignore
    commands:
      - npm install
      - ember serve
